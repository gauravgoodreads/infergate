[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -q
# Make the repo root importable so `gateway` resolves under a bare `pytest`
# invocation, not just `python -m pytest` (which adds the cwd implicitly).
pythonpath = .
